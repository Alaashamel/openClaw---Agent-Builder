# For local dev with separate Express backend:
VITE_API_URL=http://localhost:4100/api

# For Vercel production, leave VITE_API_URL empty.
# The frontend automatically uses /api on the same Vercel domain.
